---
title: test
layout: base.njk
permalink: /test/
---
{%- for post in collections.general %}
<li>
		<a href="{{post.url}}">{{ post.data.title }}</a>
		<p>{{ post.templateContent | striptags(true) | truncate(30) | safe }}</p>
</li>
{% endfor %}
</ul>